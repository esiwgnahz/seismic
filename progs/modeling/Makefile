include ${SEPINC}/SEP.top
include ../../makefile

default: ${BINDIR}/Acoustic_modeling.x ${BINDIR}/Acoustic_Born_modeling.x 

aclean: jclean       
	@-${RM} ${BINDIR}/*.x 

include ${SEPINC}/SEP.bottom
